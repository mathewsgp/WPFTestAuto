<Window x:Class="SampleWpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sample WPF App — Login" Height="260" Width="380"
        AutomationProperties.AutomationId="MainWindow">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Username"/>
        <TextBox Grid.Row="1" x:Name="txtUsername"
                 AutomationProperties.AutomationId="txtUsername"
                 Margin="0,0,0,10"/>

        <Label Grid.Row="2" Content="Password"/>
        <!-- Real apps would use PasswordBox; VisualTreeInspector.SetValue
             handles both TextBox.Text and PasswordBox.Password. Using a
             PasswordBox here to match production shape. -->
        <PasswordBox Grid.Row="3" x:Name="txtPassword"
                     AutomationProperties.AutomationId="txtPassword"
                     Margin="0,0,0,10"/>

        <StackPanel Grid.Row="4" Orientation="Vertical">
            <Button x:Name="btnSubmit" Content="Login"
                    AutomationProperties.AutomationId="btnSubmit"
                    Click="BtnSubmit_Click" Width="100" HorizontalAlignment="Left"/>
            <Label x:Name="lblError" Foreground="Red" Visibility="Collapsed" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
