<Window x:Class="WpfTestIde.Dialogs.AttachToProcessDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Attach to Process" Height="480" Width="520"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="140"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select the target application's process:" Margin="0,0,0,8" FontWeight="SemiBold"/>

        <ListView Grid.Row="1" x:Name="ProcessListView" Margin="0,0,0,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="PID" Width="70" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="Window Title" Width="260" DisplayMemberBinding="{Binding MainWindowTitle}"/>
                    <GridViewColumn Header="Process Name" Width="120" DisplayMemberBinding="{Binding ProcessName}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <TextBlock Grid.Row="2" Text="Spy Agent Named Pipe name:" Margin="0,0,0,4"/>
        <TextBox Grid.Row="3" x:Name="PipeNameBox" Text="WPFSpyAgentPipe" Margin="0,0,0,10"/>

        <GroupBox Grid.Row="4" Header="Page name mapping (window title contains -&gt; page alias)" Margin="0,0,0,10">
            <DataGrid x:Name="PageMapGrid" AutoGenerateColumns="False" CanUserAddRows="True" Margin="4">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Title contains" Binding="{Binding TitleContains}" Width="*"/>
                    <DataGridTextColumn Header="Page alias" Binding="{Binding PageAlias}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Attach" Width="80" Click="Attach_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
