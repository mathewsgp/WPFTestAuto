# Element Repository — Login Page
# XPath uses @Name to match the mock app's simple structure
# For real WPF app with Grid hierarchy, use @AutomationId instead
elements:
  LoginPage.MainWindow.txtUsername:
    displayName: "Username Text Box"
    controlType: "TextBox"
    parentAlias: "LoginPage.MainWindow"
    defaultTimeout: 10
    tags: ["login", "critical"]
    strategies:
      WPFSpy:
        searchBy: "XPath"
        value: "/Window[@Name='MainWindow']/TextBox[@Name='UsernameInput']"

  LoginPage.MainWindow.txtPassword:
    displayName: "Password Text Box"
    controlType: "PasswordBox"
    parentAlias: "LoginPage.MainWindow"
    defaultTimeout: 10
    tags: ["login", "critical"]
    strategies:
      WPFSpy:
        searchBy: "XPath"
        value: "/Window[@Name='MainWindow']/TextBox[@Name='PasswordInput']"

  LoginPage.MainWindow.btnSubmit:
    displayName: "Submit Button"
    controlType: "Button"
    parentAlias: "LoginPage.MainWindow"
    defaultTimeout: 10
    tags: ["login", "critical"]
    strategies:
      WPFSpy:
        searchBy: "XPath"
        value: "/Window[@Name='MainWindow']/Button[@Name='SubmitBtn']"

  LoginPage.MainWindow.lblError:
    displayName: "Error Label"
    controlType: "Label"
    parentAlias: "LoginPage.MainWindow"
    defaultTimeout: 5
    tags: ["login", "verification"]
    strategies:
      WPFSpy:
        searchBy: "XPath"
        value: "/Window[@Name='MainWindow']/Label[@Name='ErrorLabel']"
